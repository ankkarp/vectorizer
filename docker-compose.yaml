version: "3.8"
services:
  client:
    build: "frontend/."
    ports:
      - "3000:3000"
    command: ["npm", "run", "dev"]

  server:
    build: backend/.
    ports:
      - "8000:8000"

    command: ["python3", "-m", "backend.run"]
